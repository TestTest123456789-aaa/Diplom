<Page x:Class="BPRapp.Pages.MainMenuAdmins.Admins_List.Add"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="700" d:DesignWidth="600"
      Title="Добавить администратора">
    <Page.Resources>
        <Color x:Key="PrimaryBlue">#5B9BD5</Color>
        <Color x:Key="DarkBlue">#2E5C8A</Color>
        <Color x:Key="LightBlue">#E8F4FC</Color>
        <Color x:Key="White">#FFFFFF</Color>
        <SolidColorBrush x:Key="PrimaryBlueBrush" Color="{StaticResource PrimaryBlue}"/>
        <SolidColorBrush x:Key="DarkBlueBrush" Color="{StaticResource DarkBlue}"/>
        <SolidColorBrush x:Key="LightBlueBrush" Color="{StaticResource LightBlue}"/>
        <DropShadowEffect x:Key="CardShadow" BlurRadius="30" ShadowDepth="10" Color="#333333" Opacity="0.3" Direction="315"/>
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryBlueBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="10" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource DarkBlueBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#FAFAFA"/>
            <Setter Property="BorderBrush" Value="#DDD"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="8,0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="{StaticResource PrimaryBlueBrush}"/>
                                <Setter Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>
    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#2E8B57" Offset="0.0"/>
                <GradientStop Color="#20B2AA" Offset="0.5"/>
                <GradientStop Color="#4682B4" Offset="1.0"/>
            </LinearGradientBrush>
        </Grid.Background>
        <Canvas>
            <Ellipse Width="300" Height="300" Fill="White" Opacity="0.1" Canvas.Left="50" Canvas.Top="100"/>
            <Ellipse Width="200" Height="200" Fill="White" Opacity="0.1" Canvas.Right="100" Canvas.Bottom="150"/>
        </Canvas>
        <Border Background="White" CornerRadius="20" Width="520" Height="650" HorizontalAlignment="Center" VerticalAlignment="Center" Effect="{StaticResource CardShadow}">
            <Grid Margin="40">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,25">
                    <Border Background="{StaticResource PrimaryBlueBrush}" Width="70" Height="70" CornerRadius="35" HorizontalAlignment="Center" Margin="0,0,0,12">
                        <TextBlock Text="👤" FontSize="32" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock Text="Добавить администратора" FontSize="22" FontWeight="Bold" Foreground="{StaticResource DarkBlueBrush}" HorizontalAlignment="Center"/>
                    <TextBlock Text="Заполните данные нового пользователя" FontSize="13" Foreground="Gray" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                </StackPanel>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <StackPanel Margin="0,0,0,18">
                            <TextBlock Text="ФИО *" FontWeight="SemiBold" Foreground="#333" Margin="0,0,0,6" FontSize="13"/>
                            <TextBox x:Name="FIOTB" Style="{StaticResource ModernTextBox}" ToolTip="Введите полные ФИО администратора"/>
                        </StackPanel>
                        <StackPanel Margin="0,0,0,18">
                            <TextBlock Text="Логин *" FontWeight="SemiBold" Foreground="#333" Margin="0,0,0,6" FontSize="13"/>
                            <TextBox x:Name="loginTB" Style="{StaticResource ModernTextBox}" ToolTip="Придумайте уникальный логин"/>
                        </StackPanel>
                        <StackPanel Margin="0,0,0,18">
                            <TextBlock Text="Пароль *" FontWeight="SemiBold" Foreground="#333" Margin="0,0,0,6" FontSize="13"/>
                            <TextBox x:Name="parolTB" Style="{StaticResource ModernTextBox}" ToolTip="Минимум 8 символов, буквы и цифры"/>
                        </StackPanel>
                        <StackPanel Margin="0,0,0,18">
                            <TextBlock Text="Email" FontWeight="SemiBold" Foreground="#333" Margin="0,0,0,6" FontSize="13"/>
                            <TextBox x:Name="emailTB" Style="{StaticResource ModernTextBox}" ToolTip="Email для уведомлений и восстановления"/>
                        </StackPanel>
                        <StackPanel Margin="0,0,0,25">
                            <TextBlock Text="Номер телефона" FontWeight="SemiBold" Foreground="#333" Margin="0,0,0,6" FontSize="13"/>
                            <TextBox x:Name="phone_numberTB" Style="{StaticResource ModernTextBox}" ToolTip="Контактный номер телефона"/>
                        </StackPanel>
                        <Border Background="#FFF3CD" CornerRadius="8" Padding="12" BorderBrush="#FFD700" BorderThickness="1" Margin="0,0,0,20">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="💡" FontSize="16" Margin="0,0,8,0"/>
                                <TextBlock Text="Поля со * обязательны для заполнения" FontSize="12" Foreground="#666" TextWrapping="Wrap"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>

                <StackPanel Grid.Row="2" HorizontalAlignment="Center" Orientation="Horizontal">
                    <Button Content="← Назад" Click="GoBack" Width="120" Height="40" Margin="0,0,15,0" Background="#6C757D" Foreground="White" FontSize="13" FontWeight="SemiBold" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="10" Padding="20,10">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="#545B62"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                    <Button x:Name="BthAdd" Click="AddAdmin" Content="💾 Сохранить" Style="{StaticResource ModernButton}" Width="150"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Page>